use std::env;
// use egg::extract;

/**
 * For multiprocessing purpose (NOT FOR SINGLE PROCESS BFS)
 */
fn main() {
    let args: Vec<String> = env::args().collect();
    // extract(&args);
    return;
}
